spring:
  application:
    name: dashboard
  datasource:
    url: jdbc:h2:file:C:/Users/loren/VS-Projects/personal-dashboard/backend/data/dashboarddb;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
